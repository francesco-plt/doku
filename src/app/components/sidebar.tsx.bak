import Link from 'next/link';
import { ibmPlexMono } from '../fonts';
import { readdirSync } from 'fs';
import { join } from 'path';

const basePath = process.cwd();
const docsPath = join(basePath, 'docs');

interface SidebarProps {
    files: string[];
}

export default function Sidebar({ files }: SidebarProps) {
  return (
    <div className="p-8 flex flex-col items-center">
        <div className="p-4"></div>
        <ul>
            {files.map((file) => (
                <li key={file}>
                    <img src="/file_lines.png" className="inline-block object-scale-down px-2 pb-1 h-6" />
                    <span className={ibmPlexMono.className}>
                    {/* <Link href={`/?projectName=${projectName}&fileName=${file}`}>
                        {file}
                    </Link> */}
                    </span>
                </li>
            ))}
        </ul>
    </div>
  );
}

